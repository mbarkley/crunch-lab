# Guided-sequence reference

This standalone HTML file captures the selected interface direction for Crunch Lab: a sequence of attacks where the enemy state (`Vexed` or `Not vexed`) controls which roll path is taken, and each outcome sets the next state.

It is a visual reference only. It is not part of the React application and has no editing or calculation behavior.
